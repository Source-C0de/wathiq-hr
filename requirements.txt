# Pinned versions for reproducibility (LLM Zoomcamp rubric).
# Tested on Python 3.11.

# --- Core LLM + embeddings ---
openai==1.51.0
tiktoken==0.8.0

# --- Vector + lexical search ---
qdrant-client==1.12.1
minsearch==0.0.6
fastembed==0.4.2            # cross-encoder reranker (CPU-friendly)

# --- Ingestion (dlt) ---
dlt[filesystem,parquet]==0.5.11
pypdf==5.0.1
beautifulsoup4==4.12.3
lxml==5.3.0
requests==2.32.3
tqdm==4.66.5

# --- UI ---
streamlit==1.39.0

# --- Optional API ---
fastapi==0.115.2
uvicorn[standard]==0.31.1
pydantic==2.9.2

# --- Monitoring / data ---
pandas==2.2.3
altair==5.4.1
sqlalchemy==2.0.35

# --- Config / utils ---
python-dotenv==1.0.1
pyyaml==6.0.2